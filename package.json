{
  "name": "serp-reader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local Chrome extension that reads the Google results page you already have open.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node test/build.mjs && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.25.0",
    "linkedom": "^0.18.5",
    "typescript": "^5.6.0"
  }
}
